# Security

Hosted checkout keeps card PAN and CVV outside this application. Credentials are environment-only and tokens/secrets must never be logged. Use HTTPS, authorize payment creation in the host app, keep dependencies current, and fulfil only after verified status.
